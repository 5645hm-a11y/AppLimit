<Window x:Class="GrayscaleDesktopApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="AppLimit - Grayscale Controller" 
        Width="420" 
        Height="720"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource OnBackgroundBrush}"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="NoResize">
    
    <Window.Resources>
    </Window.Resources>

    <Grid Background="{StaticResource BackgroundBrush}">
        <!-- Splash Screen -->
        <Grid x:Name="SplashScreen" Background="{StaticResource BackgroundBrush}" Panel.ZIndex="100">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <!-- Logo with fade-in animation -->
                <Image x:Name="LogoImage" Width="120" Height="120" HorizontalAlignment="Center" Opacity="0" Margin="0,0,0,20"/>

                <!-- App Title -->
                <TextBlock x:Name="SplashTitle" Text="AppLimit" Style="{StaticResource DisplayLarge}" HorizontalAlignment="Center" Opacity="0" Margin="0,0,0,8"/>
                
                <!-- Tagline -->
                <TextBlock x:Name="SplashSubtitle" Text="Grayscale Controller" Style="{StaticResource BodyLarge}" HorizontalAlignment="Center" Opacity="0" Margin="0,0,0,32"/>
                
                <!-- Animated progress bar -->
                <StackPanel HorizontalAlignment="Center" Width="160">
                    <ProgressBar x:Name="SplashProgress" Height="3" Background="{StaticResource SurfaceVariantBrush}" Foreground="{StaticResource PrimaryBrush}" Minimum="0" Maximum="100" Value="0" Margin="0,0,0,12"/>
                    <TextBlock x:Name="InitText" Text="Initializing..." Style="{StaticResource BodySmall}" HorizontalAlignment="Center" Opacity="0.6"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Phone Frame -->
        <Border x:Name="PhoneFrame" 
                Background="{StaticResource SurfaceBrush}" 
                CornerRadius="40" 
                BorderThickness="8" 
                BorderBrush="{StaticResource OutlineBrush}"
                Width="380" 
                Height="680" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center" 
                Panel.ZIndex="50"
                Effect="{StaticResource Elevation3}">
            <Grid>
                <!-- Status Bar -->
                <Border Height="34" VerticalAlignment="Top" Background="{StaticResource BackgroundBrush}" Padding="16,8">
                    <Grid>
                        <TextBlock Text="9:41" Style="{StaticResource LabelSmall}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <TextBlock Text="📡" FontSize="11"/>
                            <TextBlock Text="🔋" FontSize="11"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Content Container -->
                <Grid Margin="0,34,0,0">
                    <!-- Step 1: Setup Instructions -->
                    <Grid x:Name="Step1" Visibility="Visible">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="32,28,32,28">
                                <!-- Icon -->
                                <TextBlock Text="📱" FontSize="64" HorizontalAlignment="Center" Opacity="0.95" Margin="0,0,0,24"/>
                                
                                <!-- Title -->
                                <TextBlock x:Name="Title1" Style="{StaticResource HeadlineMedium}" TextAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,24"/>
                                
                                <!-- Description -->
                                <TextBlock x:Name="Desc1" Style="{StaticResource BodyLarge}" TextAlignment="Center" TextWrapping="Wrap" Foreground="{StaticResource OnSurfaceBrush}" Opacity="0.8" Margin="0,0,0,24"/>
                                
                                <!-- Instructions Box -->
                                <Border Background="{StaticResource SurfaceVariantBrush}" CornerRadius="12" Padding="16,12" Margin="0,4,0,24">
                                    <StackPanel>
                                        <TextBlock x:Name="StepsLabel" Style="{StaticResource TitleMedium}" Foreground="{StaticResource PrimaryBrush}"/>
                                        <TextBlock x:Name="Instructions1" Style="{StaticResource BodyMedium}" TextWrapping="Wrap" Foreground="{StaticResource OnSurfaceBrush}" LineHeight="20"/>
                                    </StackPanel>
                                </Border>

                                <!-- Play Video Button -->
                                <Button x:Name="VideoButton" Style="{StaticResource FilledButton}" MinHeight="48" Padding="24,12" FontSize="14" Margin="0,0,0,24" Click="OnWatchVideo"/>

                                <!-- Continue Button -->
                                <Button x:Name="Step1Button" Style="{StaticResource FilledButton}" MinHeight="48" Padding="24,12" FontSize="14" Click="OnStep1Complete"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>

                    <!-- Step 2: Device Detection -->
                    <Grid x:Name="Step2" Visibility="Collapsed">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" >
                            <!-- Device Icon -->
                            <TextBlock Text="📱" FontSize="72" HorizontalAlignment="Center" Opacity="0.9" Margin="0,0,0,28"/>
                            
                            <!-- Title -->
                            <TextBlock x:Name="Title2" Style="{StaticResource HeadlineMedium}" TextAlignment="Center" Margin="0,0,0,28"/>
                            
                            <!-- Status Section -->
                            <StackPanel MinHeight="100" VerticalAlignment="Center" Margin="0,0,0,28">
                                <TextBlock x:Name="DeviceStatusText" Style="{StaticResource BodyLarge}" TextAlignment="Center" Foreground="{StaticResource OnSurfaceBrush}" Opacity="0.7" Margin="0,0,0,16"/>
                                
                                <!-- Checkmark with scale animation -->
                                <TextBlock x:Name="CheckMark" Text="✓" FontSize="56" Foreground="{StaticResource SuccessBrush}" HorizontalAlignment="Center" Opacity="0"/>
                            </StackPanel>

                            <!-- Continue Button -->
                            <Button x:Name="Step2Button" Style="{StaticResource FilledButton}" MinHeight="48" IsEnabled="False" Click="OnStep2Complete"/>
                        </StackPanel>
                    </Grid>

                    <!-- Step 3: Processing -->
                    <Grid x:Name="Step3" Visibility="Collapsed">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" >
                            <!-- Processing Icon -->
                            <TextBlock x:Name="ProcessIcon" Text="⚙️" FontSize="64" HorizontalAlignment="Center" Opacity="0.9" Margin="0,0,0,32"/>
                            
                            <!-- Title -->
                            <TextBlock x:Name="Title3" Style="{StaticResource HeadlineMedium}" TextAlignment="Center" Margin="0,0,0,32"/>
                            
                            <!-- Progress Section -->
                            <StackPanel Margin="0,0,0,32">
                                <!-- Progress Bar -->
                                <ProgressBar x:Name="ProgressBar" Height="6" Background="{StaticResource SurfaceVariantBrush}" Foreground="{StaticResource PrimaryBrush}" Value="0" Minimum="0" Maximum="100" Margin="0,0,0,18"/>
                                
                                <!-- Percentage -->
                                <TextBlock x:Name="ProgressText" Text="0%" Style="{StaticResource DisplayMedium}" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center" Margin="0,-8,0,18"/>
                                
                                <!-- Status Text -->
                                <TextBlock x:Name="ProgressStatus" Style="{StaticResource BodyMedium}" TextAlignment="Center" Foreground="{StaticResource OnSurfaceBrush}" Opacity="0.7"/>
                            </StackPanel>

                            <!-- Start Button -->
                            <Button x:Name="Step3Button" Style="{StaticResource FilledButton}" MinHeight="48" FontSize="14" Click="OnStep3Complete"/>
                        </StackPanel>
                    </Grid>

                    <!-- Step 4: Success -->
                    <Grid x:Name="Step4" Visibility="Collapsed">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" >
                            <!-- Success Icon with scale animation -->
                            <TextBlock x:Name="SuccessIcon" Text="✓" FontSize="80" Foreground="{StaticResource SuccessBrush}" HorizontalAlignment="Center" Opacity="0" Margin="0,0,0,20"/>
                            
                            <!-- Success Title -->
                            <TextBlock x:Name="Title4" Style="{StaticResource DisplayMedium}" Foreground="{StaticResource SuccessBrush}" TextAlignment="Center" Margin="0,0,0,20"/>
                            
                            <!-- Success Message -->
                            <TextBlock x:Name="Desc4" Style="{StaticResource BodyLarge}" TextAlignment="Center" TextWrapping="Wrap" Foreground="{StaticResource OnSurfaceBrush}" Opacity="0.85" Margin="0,0,0,20"/>
                            
                            <!-- Detail Box -->
                            <Border Background="{StaticResource SurfaceVariantBrush}" CornerRadius="12" Padding="14,10" Margin="0,4,0,20">
                                <TextBlock x:Name="Detail4" Style="{StaticResource BodySmall}" TextAlignment="Center" TextWrapping="Wrap" Foreground="{StaticResource OnSurfaceBrush}"/>
                            </Border>

                            <!-- Done Button -->
                            <Button x:Name="Step4Button" Style="{StaticResource FilledButton}" MinHeight="48" FontSize="14" Click="OnStep4Complete"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
